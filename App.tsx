import React from 'react';
import {SafeAreaView, ScrollView, Text, View} from 'react-native';

function App(): React.JSX.Element {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>Hello Begin</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}

export default App;
